ministrat.config.defines.map = {
  canvas_offset: [11, 27],
  pathfind_downscale_factor: 5, //How much to downscale pathfinding by, with 1 being 1px = 1km. Set to minimum range of units by default
  px_per_km: 0.9582804569,
  svg_vh_dimensions: [92, 92], //Dimensions of the main SVG in vh by default
  svg_vh_offset: [0, 0] //Nobody knows why these magic numbers are needed
};